Little pet project to try out the Google Benchmark framework.

### Instructions

#### Build the benchmark

```
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release #extra flag to avoid debug mode (significantly slower)
cmake --build .
```

