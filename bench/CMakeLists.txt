# Specify the (private) sources of the petString-bench excecutable
target_sources(petString-bench
    PRIVATE
        # Headers
        "petString-bench.h"

        # Sources
        "petString-bench.cpp"
)
